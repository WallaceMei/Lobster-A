# 🦞 龙虾雷达 v1.0 - 上线交接快照

> 给下一个 Claude 的开工说明书
> 生成: 2026-04-24 23:35 | 上线: 2026-04-24 23:25

---

## 一、项目身份

| 项 | 值 |
|---|---|
| 项目 | 龙虾雷达 (LobsterRadar) v1.0 |
| 项目根 | `C:\quant_project\龙虾雷达\` |
| GitHub | https://github.com/WallaceMei/Lobster-A |
| Wallace | 梅周涛 / 24 岁 / 英国曼彻斯特电子烟外贸 / A 股量化副业 / 无编程背景 |
| 主控 LLM | **MiniMax M2.7** (Qclaw 底模, 不是 DeepSeek) |
| 主文档 | `C:\Qclaw\docs\MASTER_PROMPT_龙虾雷达_v1.0.md` |

---

## 二、今晚完成的所有事

✅ **5 个提示词重构** — curl + Qclaw 内置 SKILL 架构 (T1/T2/T3/T4/T5)
✅ **lobster-skill 上架** — GitHub 仓库根 SKILL.md + Qclaw 技能管理已安装
✅ **Qclaw 4 个定时任务**已配:
  - T1 9:24 (竞价情报, 单次)
  - T2 9:30-10:30 每 5 min (早盘密集)
  - T3 10:30-15:00 每 10 min (盘中常规)
  - T4 15:00 (收盘三档候选)
✅ **v2.0 预留接口** — msg_id 全链路 + T4 JSON 落库扩展字段 + sqlite feedback 表
✅ **feishu_send.ps1 推送链路打通** — `-MessageFile` 方案, PS5.1 GBK 坑全绕过 (UTF8NoBom + $PSScriptRoot)
✅ **T4 测试 v3 通过** — 23:25 飞书收到 5 星完整 Markdown 报告
✅ **DSA Tushare API URL 修复** — 改 `data_provider/tushare_fetcher.py` 第 155 行读 `TUSHARE_API_URL` env, .env 加 `TUSHARE_API_URL=http://tsy.xiaodefa.cn` (**未重启不生效, 明早再重启**)
✅ **0/1/2 号样本归档** — `data/t4_candidates/2026-04-24.json`

---

## 三、关键路径速查

```bash
# DSA 启动 (常驻)
cd C:\Users\m7856\daily_stock_analysis && py -3.10 webui.py
# DSA 健康
curl --max-time 5 http://127.0.0.1:8000/api/health
# 飞书推送 (核心)
powershell -File "C:\quant_project\龙虾雷达\scripts\feishu_send.ps1" -MessageFile <path>
```

| 资产 | 路径 |
|---|---|
| 5 个提示词 | `prompts/T1-T5.md` |
| SKILL | `skills/SKILL.md` (GitHub + Qclaw 已装) |
| 推送脚本 | `scripts/feishu_send.ps1` |
| 凭据 (gitignore) | `config/secrets.env` |
| T4 落库 | `data/t4_candidates/YYYY-MM-DD.json` |
| SQLite | `data/dedupe.sqlite` (4 表) |
| DSA .env | `C:\Users\m7856\daily_stock_analysis\.env` |
| DSA TushareFetcher | `daily_stock_analysis/data_provider/tushare_fetcher.py` (line 155 已加 env 读取) |

---

## 四、关键架构决策 (避免新 Claude 走弯路)

1. ✅ **Qclaw 底模 = MiniMax M2.7** — 提示词写"你就是 M2.7"
2. ✅ **DeepSeek 只在 DSA 内部用** — T4/T5 提示词不直接调
3. ✅ **NewsNow 走 HTTP curl, 不走 MCP** — Qclaw 任务模式不加载 .claude.json mcpServers
4. ✅ **SKILL 通过 GitHub 装到 Qclaw** — 仓库根目录必须有 `SKILL.md` (不能在子目录, 不能文件名带空格)
5. ✅ **飞书推送用 `-MessageFile` 文件路径方案** — UTF-8 无 BOM (`[System.IO.File]::WriteAllText + UTF8Encoding($false)`)
6. ✅ **可用 Qclaw 内置 SKILL**: `news-summary` / `tencent-news` / `online-search` / `multi-search-engine` / `lobster-skill`
7. ❌ **tech-news-digest 真依赖 Python** — Qclaw 任务环境无 Python, 跳过
8. ❌ **AkShare / Python requests / web_fetch localhost / MCP** — Qclaw 任务环境全不可用
9. ✅ **DSA `/analyze` 实测耗时 ~150s** — T4/T5 timeout 必须 ≥ 180s
10. ✅ **PowerShell 5.1 GBK 坑** — .ps1 文件**禁含中文/emoji**, 用 `$PSScriptRoot` 推算路径
11. ✅ **Tushare 走第三方 xiaodefa** — token 来自 `tsy.xiaodefa.cn`, DSA 已 patch (待重启)

---

## 五、未来路线 (蓝图已规划)

| 版本 | 启动 | 核心 | 文档 |
|---|---|---|---|
| v2.0 | 2026-05-03+ | 反馈学习 + 命中率周报 + 本地 daemon 写 sqlite | `docs/ROADMAP_v2_v3.md` |
| v3.0 | 2026-06-01+ | 主动预判 + V3.5 联动 + 个性化偏好模型 | 同上 |

完整 4 层智能 + 3 阶段路径见 `docs/OPTIMIZATION_BACKLOG.md` §1.0

---

## 六、未解决问题 (v2.0 修复)

| 问题 | 状态 |
|---|---|
| DSA AGENT_MODE 未启用 | 不阻塞 (`/analyze` 不依赖) |
| tencent-news 需授权 | 跳过, 用 multi-search-engine 替代 |
| T4 候选无法直接落 sqlite | 临时: 飞书消息中含 JSON 块, PowerShell 抓取入库 |
| **Tushare API URL patch 待 DSA 重启** | **明早 Wallace 重启即生效** |
| Gemini key free tier=0 | 等换 key, 不影响主链路 |
| MiniMax Coding Plan 不支持 chat 模型 | 不修, M2.7 主推理已工作 |
| 详细列表 | `docs/OPTIMIZATION_BACKLOG.md` |

---

## 七、给新 Claude 的开工建议

1. **先读** `BLOCKERS.md` (不要重复诊断已解决问题)
2. **遇 Qclaw 配置问题** 看 `docs/部署指南.md` Step C.5 数据源协议
3. **遇推送/调试问题** 看 `docs/调试手册.md` §4.4-4.6
4. **改提示词前** 用 grep 看 yaml 头, 当前底模是 M2.7 不要又改成 DeepSeek
5. **Wallace 风格**: 直接 / 工程优先 / 决策快 / 中文 / 不要客套 / 技术细节 CC 自决, 大方向 Wallace 拍板
6. **重启 DSA** 后看启动日志应有 `Tushare API 初始化成功` + `Priority -1`, 没有 `your_xxx_key_here` 试错

🦞 **let's go.**
